<?php
/** @var  Ls\Customer\Block\Order\Info|Magento\Framework\View\Element\Template $block */
/** @var  Ls\Hospitality\ViewModel\CustomerOrderInfo $viewModel */

$viewModel = $block->getData("view_model");
if ($viewModel->isHospitalityEnabled() == "1") :
    $ajaxUrl         = $block->getUrl('hospitality/ajax/OrderInfo');
    $order           = $block->getOrder();
    $statusInfo      = $viewModel->getOrderStatusInfo($order);
    $magOrder        = $block->getMagOrder();
    $orderId         = null;
    $pickupOrderTime = '';
    $orderComment    = '';
    $storeId         = '';
    $refreshInterval = $viewModel->getRefreshInterval(); // Get interval from admin config

    if (!empty($statusInfo)) {
        $orderId         = $statusInfo['orderId'];
        $storeId         = $statusInfo['storeId'];
        $pickupOrderTime = $statusInfo['pickupDateTimeslot'];
    }
    if (!empty($order) && empty($statusInfo)) {
        $orderId = $order->getId();
        $storeId = $order->getStoreId();
    }
    if (!empty($magOrder)) {
        $pickupOrderTime = $magOrder->getPickupDateTimeslot();
        $orderComment    = $magOrder->getLsOrderComment();
    }
    if ($orderId):
        ?>
        <div class="block block-order-details-view-loyalty ls-kitchen-order-status">
            <div class="block-title">
                <strong><?= $block->escapeHtml(__('Kitchen Information')) ?></strong>
            </div>
            <div id="ls-hosp-order-info">
                <div class="loader">
                    <img src="<?= $block->escapeUrl($block->getViewFileUrl('images/loader-1.gif')) ?>"
                         alt="<?= $block->escapeHtmlAttr(__('Loading...')) ?>">
                    <br/>
                    <div><?= $block->escapeHtml(__('Loading Kitchen Info...')) ?></div>
                </div>
            </div>
        </div>
        <script type="text/x-magento-init">
            {
                "*": {
                    "Ls_Hospitality/js/view/customer/order/info": {
                        "ajaxUrl": "<?= $block->escapeUrl($ajaxUrl) ?>",
                        "orderId": "<?= $block->escapeHtmlAttr($orderId) ?>",
                        "storeId": "<?= $block->escapeHtmlAttr($storeId) ?>",
                        "pickupOrderTime": "<?= $block->escapeHtmlAttr($pickupOrderTime) ?>",
                        "autoRefreshEnabled": <?= $viewModel->isAutoRefreshEnabled() ? 'true' : 'false' ?>,
                        "refreshInterval": <?= $refreshInterval ?>
                    }
                }
            }
        </script>
        <?php if ($orderComment): ?>
        <div class="block block-order-details-view-loyalty">
            <div class="block-title">
                <strong><?= $block->escapeHtml(__('Additional Information')) ?></strong>
            </div>
            <div class="block-content">
                <div class="custom-box">
                    <span><?= $block->escapeHtml(nl2br($orderComment)) ?></span>
                </div>
            </div>
        </div>
    <?php endif; ?>
    <?php endif; ?>
<?php endif; ?>